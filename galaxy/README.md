Files used to generate dockerized version of kstar.

Copy calculate, mapping, and helpers modules. In addition, copy the resource files and network files.

To make docker image, run:
docker build -t kstar-galaxy_full .
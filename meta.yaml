{% set name = "kstar" %}
{% set version = "0.4.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/kstar-{{ version }}.tar.gz
  sha256: a11e4e5e79f7ed10f8c68f8c2c115fa13ade55069677b0bfb60b64f444b23a69

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - biopython
    - matplotlib-base
    - numpy
    - pandas
    - python >=3.6
    - requests
    - scipy
    - seaborn
    - statsmodels

test:
  imports:
    - kstar
    - kstar.normalize
    - kstar.validate
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NaegleLab/KSTAR
  summary: 'KSTAR: Kinase-Substrate Transfer to Activity Relationships'
  license: GPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - NaegleLab
